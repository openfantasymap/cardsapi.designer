# URL of the ccc-server backend (no trailing slash).
# Set this in the Netlify environment variables dashboard before deploying.
# In local dev, requests fall back to /api on the same host if this is unset.
VITE_CARDFORGE_API_URL=https://your-ccc-server.example.com
